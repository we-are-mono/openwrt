config check 'check'
	# Server base URL (the directory holding latest.json).
	# Empty disables checking.
	option url 'https://openwrt.mono.si'
	# notify: log availability only. auto: download, verify, sysupgrade.
	option mode 'notify'
