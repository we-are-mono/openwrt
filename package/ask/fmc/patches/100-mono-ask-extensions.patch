diff --git a/source/FMCCModelOutput.cpp b/source/FMCCModelOutput.cpp
index 2a3d257..54b6f21 100644
--- a/source/FMCCModelOutput.cpp
+++ b/source/FMCCModelOutput.cpp
@@ -725,6 +725,7 @@ CFMCCModelOutput::output_fmc_port( const CFMCModel& model, fmc_model_t* cmodel,
 
     EMIT4STR( port[, index, ].type =, model.all_ports[index].type );
     EMIT4( port[, index, ].number  = ,  model.all_ports[index].number );
+    EMIT4( port[, index, ].portid  = ,  model.all_ports[index].portid );
 
     EMIT4( port[, index, ].schemes_count =, model.all_ports[index].schemes.size() );
     for ( unsigned int i = 0; i < cmodel->port[index].schemes_count; ++i ) {
@@ -952,6 +953,7 @@ CFMCCModelOutput::output_fmc_scheme( const CFMCModel& model, fmc_model_t* cmodel
     unsigned int useHash = sch.key.empty() ? 0 : 1;
     EMIT4( scheme[, index, ].useHash =,  useHash );
     EMIT4( scheme[, index, ].baseFqid =, sch.qbase );
+    EMIT4( scheme[, index, ].shared =, sch.shared );
     if (sch.bypass) {
         EMIT4( scheme[, index, ].bypassFqidGeneration =, sch.bypass );
     }
diff --git a/source/FMCGenericError.cpp b/source/FMCGenericError.cpp
index 0bbd284..5ba2f2b 100644
--- a/source/FMCGenericError.cpp
+++ b/source/FMCGenericError.cpp
@@ -33,7 +33,11 @@
 #include "FMCUtils.h"
 #include "logger.hpp"
 
+#if LIBXML_VERSION >= 21200
 void errorFuncHandler( void * ctx, const xmlError * error )
+#else
+void errorFuncHandler( void * ctx, xmlError * error )
+#endif
 {
     char *filestr = (char*)"";
     char *msgstr  = (char*)"";
diff --git a/source/FMCGenericError.h b/source/FMCGenericError.h
index 4bd05b2..ddefce1 100644
--- a/source/FMCGenericError.h
+++ b/source/FMCGenericError.h
@@ -32,10 +32,15 @@
 
 #include <string>
 #include <libxml/parser.h>
+#include <libxml/xmlversion.h>
 
 const int NO_LINE = -1;
 
+#if LIBXML_VERSION >= 21200
 void errorFuncHandler( void * ctx, const xmlError * error );
+#else
+void errorFuncHandler( void * ctx, xmlError * error );
+#endif
 
 
 class CGenericError {
diff --git a/source/FMCPCDModel.cpp b/source/FMCPCDModel.cpp
index d785eee..bb6435f 100755
--- a/source/FMCPCDModel.cpp
+++ b/source/FMCPCDModel.cpp
@@ -133,12 +133,129 @@ CFMCModel::CFMCModel()
 }
 
 
+HTNode&
+CFMCModel::replicateHtNodes(const CTaskDef* pTaskDef, Port& port, HTNode& refnode)
+{
+    uint32_t ii;
+    uint32_t index;
+
+    for( ii = 0; ii < port.htnodes.size(); ii++) {
+	if (all_htnodes[port.htnodes[ii]].name == refnode.name) {
+		return all_htnodes[port.htnodes[ii]];
+	}
+    }
+
+    HTNode& htNode = all_htnodes[FMBlock::assignIndex( all_htnodes )];
+    ApplyOrder::Entry n1( ApplyOrder::HTNode, htNode.getIndex() );
+    applier.add_edge( n1, ApplyOrder::Entry( ApplyOrder::None, 0 ) );
+    port.htnodes.push_back(htNode.getIndex() );
+
+    htNode.name = refnode.name;
+    htNode.maxNumOfKeys = refnode.maxNumOfKeys;
+    htNode.statistics = refnode.statistics;
+    htNode.hashResMask = refnode.hashResMask;
+    htNode.matchKeySize = refnode.matchKeySize;
+    htNode.hashShift = refnode.hashShift;
+    htNode.kgHashShift = refnode.kgHashShift;
+    for (ii = 0; ii < refnode.keys.size(); ii++) {
+    	htNode.keys.push_back(HTNode::CCData());
+    }
+    for (ii = 0; ii < refnode.masks.size(); ii++) {
+    	htNode.masks.push_back(HTNode::CCData());
+    }
+    for (ii = 0; ii < refnode.nextEngines.size(); ii++) {
+    	htNode.nextEngines.push_back(HTNode::CCNextEngine());
+    }
+
+    for (ii = 0; ii < refnode.frag.size(); ii++) {
+	index = refnode.frag[ii];
+	htNode.frag.push_back(index);
+    }
+    for (ii = 0; ii < refnode.header.size(); ii++) {
+	index = refnode.header[ii];
+    	htNode.header.push_back(index);
+    }
+    for (ii = 0; ii < refnode.indices.size(); ii++) {
+	index = refnode.indices[ii];
+    	htNode.indices.push_back(index);
+    }
+
+    htNode.nextEngineOnMiss = refnode.nextEngineOnMiss;
+    htNode.fragOnMiss = refnode.fragOnMiss;
+    htNode.headerOnMiss = refnode.headerOnMiss;
+    htNode.port_signature = port.name;
+
+    port.cctrees.push_back( htNode.getIndex() );
+    port.cctrees_type.push_back( e_FM_PCD_HASH );
+    port.hdrmanips.push_back( 0 );
+    return htNode;
+}
+
+CCNode&
+CFMCModel::replicateCCNodes(const CTaskDef* pTaskDef, Port& port, CCNode& refnode)
+{
+    uint32_t ii;
+    uint32_t index;
+
+    CCNode& ccNode = all_ccnodes[FMBlock::assignIndex( all_ccnodes )];
+
+    ApplyOrder::Entry n1( ApplyOrder::CCNode, ccNode.getIndex() );
+    applier.add_edge( n1, ApplyOrder::Entry( ApplyOrder::None, 0 ) );
+
+    port.ccnodes.push_back(ccNode.getIndex() );
+
+    ccNode.name = refnode.name;
+    ccNode.maxNumOfKeys = refnode.maxNumOfKeys;
+    ccNode.statistics = refnode.statistics;
+    ccNode.maskSupport = refnode.maskSupport;
+    ccNode.shared = 0;
+    ccNode.extract = refnode.extract;
+    ccNode.keySize = refnode.keySize;
+    ccNode.used_protocols.insert(refnode.used_protocols.begin(), refnode.used_protocols.end());
+    for (ii = 0; ii < refnode.keys.size(); ii++) {
+    	ccNode.keys.push_back(CCNode::CCData());
+    }
+    for (ii = 0; ii < refnode.masks.size(); ii++) {
+    	ccNode.masks.push_back(CCNode::CCData());
+    }
+    for (ii = 0; ii < refnode.nextEngines.size(); ii++) {
+    	ccNode.nextEngines.push_back(CCNode::CCNextEngine());
+    }
+    for (ii = 0; ii < refnode.frag.size(); ii++) {
+	index = refnode.frag[ii];
+	ccNode.frag.push_back(index);
+    }
+    for (ii = 0; ii < refnode.header.size(); ii++) {
+	index = refnode.header[ii];
+    	ccNode.header.push_back(index);
+    }
+    for (ii = 0; ii < refnode.indices.size(); ii++) {
+	index = refnode.indices[ii];
+    	ccNode.indices.push_back(index);
+    }
+    for (ii = 0; ii < refnode.frameLength.size(); ii++) {
+	index = refnode.frameLength[ii];
+    	ccNode.frameLength.push_back(index);
+    }
+    ccNode.nextEngineOnMiss = refnode.nextEngineOnMiss;
+    ccNode.fragOnMiss = refnode.fragOnMiss;
+    ccNode.headerOnMiss = refnode.headerOnMiss;
+    ccNode.port_signature = port.name;
+    port.cctrees.push_back( ccNode.getIndex() );
+    port.cctrees_type.push_back( e_FM_PCD_CC );
+    port.hdrmanips.push_back( 0 );
+    return ccNode;
+}
+
+
 ////////////////////////////////////////////////////////////////////////////////
 /// Create model by building the internal database
 ////////////////////////////////////////////////////////////////////////////////
 bool
 CFMCModel::createModel( CTaskDef* pTaskDef )
 {
+    uint32_t ii;
+    uint32_t jj;
     assert( pTaskDef );
 
     // For all engines
@@ -192,6 +309,23 @@ CFMCModel::createModel( CTaskDef* pTaskDef )
 
                 Scheme& scheme = all_schemes[index];
                 scheme.scheme_index_per_port = scheme_index;
+		if (scheme.shared && (port.name != scheme.port_signature)) {
+			 if (scheme.nextEngine == e_FM_PCD_CC) {
+                         	std::map< std::string, CClassification >::const_iterator nodeIt;
+                                nodeIt = pTaskDef->classifications.find(scheme.nextActionName);
+                                if ( nodeIt == pTaskDef->classifications.end() ) {
+                        	        throw CGenericError( ERR_CC_NOT_FOUND, scheme.nextActionName,
+                                		scheme.name );
+                                }
+                                if ( nodeIt->second.key.hashTable ) {
+                                	HTNode& refnode = all_htnodes[scheme.actionHandleIndex];
+                                	CFMCModel::replicateHtNodes(pTaskDef, port, refnode);
+                                } else {
+                                	CCNode& refnode = all_ccnodes[scheme.actionHandleIndex];
+                                	CFMCModel::replicateCCNodes(pTaskDef, port, refnode);
+                                }
+                	}
+		}
 
                 // Add scheme's protocols to the port's protocol list
                 std::set< Protocol >::iterator protoIt;
@@ -1011,6 +1145,7 @@ CFMCModel::createScheme( const CTaskDef* pTaskDef, Port& port, const CDistributi
     // Add next engine entry
     scheme.nextEngine        = getEngineByType( xmlDist.action );
     scheme.nextEngineStr     = getEngineByTypeStr( xmlDist.action );
+    scheme.nextActionName    = xmlDist.actionName;
     scheme.doneAction        = e_FM_PCD_ENQ_FRAME;
     scheme.doneActionStr     = "e_FM_PCD_ENQ_FRAME";
     scheme.actionHandleIndex = 0xFFFFFFFF;
@@ -2920,7 +3055,7 @@ CFMCModel::getNetCommFieldType( std::string fieldname )
     fields["pppoe.code"]          = NET_HEADER_FIELD_PPPoE_CODE;
     fields["pppoe.session_ID"]    = NET_HEADER_FIELD_PPPoE_SID;
     fields["pppoe.hlen"]          = NET_HEADER_FIELD_PPPoE_LEN;
-    fields["pppoe.nextp"]         = NET_HEADER_FIELD_PPP_PID;
+    fields["pppoe.nextp"]         = NET_HEADER_FIELD_PPPoE_PID;
     fields["minencap.dst"]        = NET_HEADER_FIELD_MINENCAP_DST_IP;
     fields["sctp.sport"]          = NET_HEADER_FIELD_SCTP_PORT_SRC;
     fields["sctp.dport"]          = NET_HEADER_FIELD_SCTP_PORT_DST;
@@ -3007,7 +3142,7 @@ CFMCModel::getNetCommFieldTypeStr( std::string fieldname )
     fields["pppoe.code"]          = "NET_HEADER_FIELD_PPPoE_CODE";
     fields["pppoe.session_ID"]    = "NET_HEADER_FIELD_PPPoE_SID";
     fields["pppoe.hlen"]          = "NET_HEADER_FIELD_PPPoE_LEN";
-    fields["pppoe.nextp"]         = "NET_HEADER_FIELD_PPP_PID";
+    fields["pppoe.nextp"]         = "NET_HEADER_FIELD_PPPoE_PID";
     fields["minencap.dst"]        = "NET_HEADER_FIELD_MINENCAP_DST_IP";
     fields["sctp.sport"]          = "NET_HEADER_FIELD_SCTP_PORT_SRC";
     fields["sctp.dport"]          = "NET_HEADER_FIELD_SCTP_PORT_DST";
diff --git a/source/FMCPCDModel.h b/source/FMCPCDModel.h
index 696eb79..2789d52 100755
--- a/source/FMCPCDModel.h
+++ b/source/FMCPCDModel.h
@@ -192,6 +192,7 @@ public:
 
     e_FmPcdEngine     nextEngine;
     std::string       nextEngineStr;
+    std::string       nextActionName;
     e_FmPcdDoneAction doneAction;
     std::string       doneActionStr;
     unsigned int      actionHandleIndex;
@@ -604,6 +605,8 @@ private:
 #endif /* (DPAA_VERSION >= 11) */
     unsigned int get_policer_index( const CTaskDef* pTaskDef, std::string name,
                                     std::string from, Port& port );
+    CCNode&  replicateCCNodes(const CTaskDef* pTaskDef, Port& port, CCNode& refnode);
+    HTNode&  replicateHtNodes(const CTaskDef* pTaskDef, Port& port, HTNode& refnode);
 };
 
 #endif // FMCMODEL_H
diff --git a/source/FMCPDLReader.cpp b/source/FMCPDLReader.cpp
index f8eb076..a7970a4 100644
--- a/source/FMCPDLReader.cpp
+++ b/source/FMCPDLReader.cpp
@@ -116,7 +116,10 @@ CPDLReader::parseNetPDL( std::string filename )
     // LibXML preparations
     xmlInitParser();
     // init line numbers - critical for Linux
+    // (no-op and deprecated in libxml2 2.12+, where line numbers are always on)
+#if LIBXML_VERSION < 21200
     xmlLineNumbersDefault(1);
+#endif
 
     LIBXML_TEST_VERSION;
     xmlSetStructuredErrorFunc( &error, errorFuncHandler );
diff --git a/source/fmc.h b/source/fmc.h
index 700ce41..5baf366 100644
--- a/source/fmc.h
+++ b/source/fmc.h
@@ -42,7 +42,7 @@ extern "C"
 #include <Peripherals/fm_pcd_ext.h>
 #include <Peripherals/fm_port_ext.h>
 
-#define FMC_OUTPUT_FORMAT_VER 0x106
+#define FMC_OUTPUT_FORMAT_VER 0x108
 
 #define FMC_NAME_LEN             64
 #define FMC_FMAN_NUM              2
@@ -102,6 +102,7 @@ typedef struct fmc_fman_t {
 typedef struct fmc_port_t {
     e_FmPortType         type;                      ///< Port type
     unsigned int         number;                    ///< Port number
+    unsigned int         portid;                    ///< Port identification from xml file
     t_FmPcdNetEnvParams  distinctionUnits;          ///< Port's network env
     t_FmPortPcdParams    pcdParam;
     t_FmPortPcdPrsParams prsParam;
@@ -110,6 +111,12 @@ typedef struct fmc_port_t {
     char                 name[FMC_NAME_LEN];
     char                 cctree_name[FMC_NAME_LEN];
     t_Handle             handle;
+    /* Runtime acquisition state used by partial-startup cleanup. */
+    unsigned int         pcd_set;
+    unsigned int         port_disabled;
+    unsigned int         tx_port_disabled;
+    bool                 port_was_enabled;
+    bool                 tx_port_was_enabled;
     t_Handle             env_id_handle;
     t_Handle             env_id_devId; 				///< handle to net environment allocated by the driver in kernel
     t_Handle             cctree_handle;
diff --git a/source/fmc_exec.c b/source/fmc_exec.c
index c105037..e44f227 100755
--- a/source/fmc_exec.c
+++ b/source/fmc_exec.c
@@ -271,6 +271,14 @@ fmc_clean( fmc_model* model )
         }
     }
 
+    if ( ret != 0 ) {
+        for ( i = 0; i < model->fman_count; i++ ) {
+            if ( model->fman[i].pcd_handle != 0 &&
+                 FM_PCD_AllowHcUsage( model->fman[i].pcd_handle, TRUE ) != E_OK )
+                fmc_log_write( LOG_ERR, "Unable to restore HC usage for %s",
+                               model->fman[i].pcd_name );
+        }
+    }
     return ret;
 }
 
@@ -562,6 +570,7 @@ fmc_exec_port_start( fmc_model* model, unsigned int engine, unsigned int port )
 
 	if (portShare > -1) {
 		pport->env_id_handle = model->port[portShare].env_id_handle;
+        pport->env_id_devId = model->port[portShare].env_id_devId;
 	} else {
 		LOG_FMD_CALL( FM_PCD_NetEnvCharacteristicsSet, model->port[port].name );
 		pport->env_id_handle = FM_PCD_NetEnvCharacteristicsSet(
@@ -588,6 +597,7 @@ fmc_exec_port_end( fmc_model* model, unsigned int engine, unsigned int port )
     unsigned int action_index;
     unsigned int reasm_index = 0;
     unsigned int port_ht_index = 0;
+    t_Handle scheme_handle;
 
 #ifndef P1023
     reasm_index = model->port[port].reasm_index;
@@ -616,10 +626,11 @@ fmc_exec_port_end( fmc_model* model, unsigned int engine, unsigned int port )
 			model->scheme[index].id.h_Scheme = model->scheme_handle[index];
 
 		    LOG_FMD_CALL( FM_PCD_KgSchemeSet, model->scheme_name[index] );
-		    model->scheme_handle[index] = FM_PCD_KgSchemeSet( model->fman[engine].pcd_handle,
+		    scheme_handle = FM_PCD_KgSchemeSet( model->fman[engine].pcd_handle,
                             &(model->scheme[index]) );
 		    CHECK_HANDLE( FM_PCD_KgSchemeSet, model->scheme_name[index],
-                  model->scheme_handle[index] );
+                  scheme_handle );
+            model->scheme_handle[index] = scheme_handle;
 
 		    model->scheme_devId[index] = GetDeviceId(model->scheme_handle[index]);
 
@@ -697,21 +708,25 @@ fmc_exec_port_end( fmc_model* model, unsigned int engine, unsigned int port )
             err = FM_PCD_HashTableAddKey( model->htnode_handle[index], 
                                 model->htnode[index].matchKeySize,
                                 &(model->htentry[index][i]) );
-            /*CHECK_HANDLE( FM_PCD_HashTableAddKey, model->htnode_name[index], !res ); */
+            CHECK_ERR( FM_PCD_HashTableAddKey, model->htnode_name[index] );
         }
     }
     /* End of adding HT entries */
 
-    LOG_FMD_CALL( FM_PORT_Disable, pport->name );
+    err = FM_PORT_GetEnabled( pport->handle, &pport->port_was_enabled );
+    CHECK_ERR( FM_PORT_GetEnabled, pport->name );
+    pport->port_disabled = 1;
     err = FM_PORT_Disable( pport->handle );
-    CHECK_ERR( FM_PORT_Disable, model->port[port].name );
+    CHECK_ERR( FM_PORT_Disable, pport->name );
 
 #if (DPAA_VERSION >= 11)
     if (pport->vspParam.numOfProfiles > 0) {
         if (pport->type != e_FM_PORT_TYPE_OH_OFFLINE_PARSING) {
-            LOG_FMD_CALL( FM_PORT_Disable, pport->name );
+            err = FM_PORT_GetEnabled( pport->vspParam.h_FmTxPort, &pport->tx_port_was_enabled );
+            CHECK_ERR( FM_PORT_GetEnabled, pport->name );
+            pport->tx_port_disabled = 1;
             err = FM_PORT_Disable( pport->vspParam.h_FmTxPort );
-            CHECK_ERR( FM_PORT_Disable, model->port[port].name );
+            CHECK_ERR( FM_PORT_Disable, pport->name );
         }
     LOG_FMD_CALL( FM_PORT_VSPAlloc, pport->name );
     err = FM_PORT_VSPAlloc( pport->handle, &pport->vspParam );
@@ -722,16 +737,21 @@ fmc_exec_port_end( fmc_model* model, unsigned int engine, unsigned int port )
     LOG_FMD_CALL( FM_PORT_SetPCD, pport->name );
     err = FM_PORT_SetPCD( pport->handle, &pport->pcdParam );
     CHECK_ERR( FM_PORT_SetPCD, model->port[port].name );
-    LOG_FMD_CALL( FM_PORT_Enable, pport->name );
-    err = FM_PORT_Enable( pport->handle );
-    CHECK_ERR( FM_PORT_Enable, model->port[port].name );
+    pport->pcd_set = 1;
+    if ( pport->port_was_enabled ) {
+        err = FM_PORT_Enable( pport->handle );
+        CHECK_ERR( FM_PORT_Enable, pport->name );
+    }
+    pport->port_disabled = 0;
 
 #if (DPAA_VERSION >= 11)
     if (pport->vspParam.numOfProfiles > 0) {
         if (pport->type != e_FM_PORT_TYPE_OH_OFFLINE_PARSING) {
-            LOG_FMD_CALL( FM_PORT_Enable, pport->name );
-            err = FM_PORT_Enable( pport->vspParam.h_FmTxPort );
-            CHECK_ERR( FM_PORT_Enable, model->port[port].name );
+            if ( pport->tx_port_was_enabled ) {
+                err = FM_PORT_Enable( pport->vspParam.h_FmTxPort );
+                CHECK_ERR( FM_PORT_Enable, pport->name );
+            }
+            pport->tx_port_disabled = 0;
         }
     }
 #endif /* (DPAA_VERSION >= 11) */
@@ -1146,6 +1166,7 @@ fmc_clean_engine_start( fmc_model* model, unsigned int index )
             LOG_FMD_CALL( FM_PCD_ManipNodeDelete, model->fman[index].frag_name[model->fman[index].frag_count - i - 1] );
             err = FM_PCD_ManipNodeDelete( model->fman[index].frag_handle[model->fman[index].frag_count - i - 1] );
             CHECK_ERR( FM_PCD_ManipNodeDelete, model->fman[index].frag_name[model->fman[index].frag_count - i - 1] );
+            model->fman[index].frag_handle[model->fman[index].frag_count - i - 1] = 0;
         }
     }
 
@@ -1154,26 +1175,26 @@ fmc_clean_engine_start( fmc_model* model, unsigned int index )
             LOG_FMD_CALL( FM_PCD_ManipNodeDelete, model->fman[index].reasm_name[model->fman[index].reasm_count - i - 1] );
             err = FM_PCD_ManipNodeDelete( model->fman[index].reasm_handle[model->fman[index].reasm_count - i - 1] );
             CHECK_ERR( FM_PCD_ManipNodeDelete, model->fman[index].reasm_name[model->fman[index].reasm_count - i - 1] );
+            model->fman[index].reasm_handle[model->fman[index].reasm_count - i - 1] = 0;
         }
     }
 
 #endif /* P1023 */
 
-    /* fmc_clean_engine_start is called last:
-    * Restore HC usage to be allowed */
-    LOG_FMD_CALL( FM_PCD_AllowHcUsage, model->fman[index].pcd_name );
-    err = FM_PCD_AllowHcUsage( model->fman[index].pcd_handle, TRUE );
-    CHECK_ERR( FM_PCD_AllowHcUsage, model->fman[index].pcd_name );
-
     if ( model->fman[index].pcd_handle != 0 ) {
+        LOG_FMD_CALL( FM_PCD_AllowHcUsage, model->fman[index].pcd_name );
+        err = FM_PCD_AllowHcUsage( model->fman[index].pcd_handle, TRUE );
+        CHECK_ERR( FM_PCD_AllowHcUsage, model->fman[index].pcd_name );
 #ifndef NETCOMM_SW
         FM_PCD_Close( model->fman[index].pcd_handle );
 #endif
+        model->fman[index].pcd_handle = 0;
     }
     if ( model->fman[index].handle != 0 ) {
 #ifndef NETCOMM_SW
         FM_Close( model->fman[index].handle );
 #endif
+        model->fman[index].handle = 0;
     }
 
     return 0;
@@ -1186,6 +1207,9 @@ fmc_clean_engine_end( fmc_model* model, unsigned int index )
 {
     t_Error err;
 
+    if ( model->fman[index].pcd_handle == 0 )
+        return 0;
+
     /* fmc_clean_engine_end is called first:
      * Temporary Deny HC usage
      * to avoid crash of certain HC operations performed under heavy traffic */
@@ -1206,20 +1230,34 @@ static int
 fmc_clean_port_start( fmc_model* model, unsigned int engine, unsigned int port )
 {
     fmc_port* pport = &model->port[port];
-    t_Error   err;
+    t_Error err;
+    unsigned int i;
 
-    if ( pport->handle == 0 ) {
-        return 0;
+    if ( pport->env_id_handle != 0 ) {
+        /* Shared schemes also share the net environment. The last port
+         * holding its wrapper releases it, after all users are detached. */
+        for ( i = 0; i < model->port_count; i++ ) {
+            if ( i != port &&
+                 model->port[i].env_id_handle == pport->env_id_handle )
+                break;
+        }
+        if ( i == model->port_count ) {
+            err = FM_PCD_NetEnvCharacteristicsDelete( pport->env_id_handle );
+            CHECK_ERR( FM_PCD_NetEnvCharacteristicsDelete, pport->name );
+        }
+        pport->env_id_handle = 0;
     }
-
-    LOG_FMD_CALL( FM_PCD_NetEnvCharacteristicsDelete, pport->name );
-    err = FM_PCD_NetEnvCharacteristicsDelete( pport->env_id_handle );
-    CHECK_ERR( FM_PCD_NetEnvCharacteristicsDelete, pport->name );
-
 #ifndef NETCOMM_SW
-    FM_PORT_Close( pport->handle );
+#if (DPAA_VERSION >= 11)
+    if ( pport->vspParam.h_FmTxPort != 0 ) {
+        FM_PORT_Close( pport->vspParam.h_FmTxPort );
+        pport->vspParam.h_FmTxPort = 0;
+    }
 #endif
-
+    if ( pport->handle != 0 )
+        FM_PORT_Close( pport->handle );
+#endif
+    pport->handle = 0;
     return 0;
 }
 
@@ -1231,19 +1269,36 @@ fmc_clean_port_end( fmc_model* model, unsigned int engine, unsigned int port )
     t_Error err;
     fmc_port* pport = &model->port[port];
 
-    if ( pport->handle == 0 ) {
-        return 0;
+    if ( pport->pcd_set ) {
+        if ( !pport->port_disabled ) {
+            err = FM_PORT_GetEnabled( pport->handle, &pport->port_was_enabled );
+            CHECK_ERR( FM_PORT_GetEnabled, pport->name );
+            pport->port_disabled = 1;
+        }
+        err = FM_PORT_Disable( pport->handle );
+        CHECK_ERR( FM_PORT_Disable, pport->name );
+        err = FM_PORT_DeletePCD( pport->handle );
+        CHECK_ERR( FM_PORT_DeletePCD, pport->name );
+        pport->pcd_set = 0;
     }
-
-    LOG_FMD_CALL( FM_PORT_Disable, pport->name );
-    err = FM_PORT_Disable( pport->handle );
-    CHECK_ERR( FM_PORT_Disable, pport->name );
-
-    LOG_FMD_CALL( FM_PORT_DeletePCD, pport->name );
-    err = FM_PORT_DeletePCD( pport->handle );
-    CHECK_ERR( FM_PORT_DeletePCD, pport->name );
-    if ( err ) { return 6; }
-
+    /* Restore the state captured before this load or cleanup stopped the
+     * port. A port held down by the stack must remain down. */
+    if ( pport->port_disabled ) {
+        if ( pport->port_was_enabled ) {
+            err = FM_PORT_Enable( pport->handle );
+            CHECK_ERR( FM_PORT_Enable, pport->name );
+        }
+        pport->port_disabled = 0;
+    }
+#if (DPAA_VERSION >= 11)
+    if ( pport->tx_port_disabled ) {
+        if ( pport->tx_port_was_enabled ) {
+            err = FM_PORT_Enable( pport->vspParam.h_FmTxPort );
+            CHECK_ERR( FM_PORT_Enable, pport->name );
+        }
+        pport->tx_port_disabled = 0;
+    }
+#endif
     return 0;
 }
 
@@ -1258,6 +1313,7 @@ fmc_clean_scheme( fmc_model* model,  unsigned int engine,
         LOG_FMD_CALL( FM_PCD_KgSchemeDelete, model->scheme_name[index] );
         err = FM_PCD_KgSchemeDelete( model->scheme_handle[index] );
         CHECK_ERR( FM_PCD_KgSchemeDelete, model->scheme_name[index] );
+        model->scheme_handle[index] = 0;
     }
 
     return 0;
@@ -1274,6 +1330,7 @@ fmc_clean_ccnode( fmc_model* model, unsigned int engine,
         LOG_FMD_CALL( FM_PCD_MatchTableDelete, model->ccnode_name[index] );
         err = FM_PCD_MatchTableDelete( model->ccnode_handle[index] );
         CHECK_ERR( FM_PCD_MatchTableDelete, model->ccnode_name[index] );
+        model->ccnode_handle[index] = 0;
     }
 
     return 0;
@@ -1289,6 +1346,7 @@ fmc_clean_htnode( fmc_model* model, unsigned int engine,
         LOG_FMD_CALL( FM_PCD_HashTableDelete, model->htnode_name[index] );
         err = FM_PCD_HashTableDelete( model->htnode_handle[index] );
         CHECK_ERR( FM_PCD_HashTableDelete, model->htnode_name[index] );
+        model->htnode_handle[index] = 0;
     }
 
     return 0;
@@ -1305,6 +1363,7 @@ fmc_clean_cctree( fmc_model* model, unsigned int engine,
         LOG_FMD_CALL( FM_PCD_CcRootDelete, model->port[port].name );
         err = FM_PCD_CcRootDelete( model->port[port].cctree_handle );
         CHECK_ERR( FM_PCD_CcRootDelete, model->port[port].name );
+        model->port[port].cctree_handle = 0;
     }
 
     return 0;
@@ -1321,6 +1380,7 @@ fmc_clean_policer( fmc_model* model, unsigned int engine,
         LOG_FMD_CALL( FM_PCD_PlcrProfileDelete, model->policer_name[index] );
         err = FM_PCD_PlcrProfileDelete( model->policer_handle[index] );
         CHECK_ERR( FM_PCD_PlcrProfileDelete, model->policer_name[index] );
+        model->policer_handle[index] = 0;
     }
 
     return 0;
@@ -1337,6 +1397,7 @@ fmc_clean_manip( fmc_model* model, unsigned int engine,
         LOG_FMD_CALL( FM_PCD_ManipNodeDelete, model->fman[engine].hdr_name[index] );
         err = FM_PCD_ManipNodeDelete( model->fman[engine].hdr_handle[index] );
         CHECK_ERR( FM_PCD_ManipNodeDelete, model->fman[engine].hdr_name[index] );
+        model->fman[engine].hdr_handle[index] = 0;
     }
 #endif /* P1023 */
 
@@ -1354,6 +1415,7 @@ fmc_clean_replicator( fmc_model* model, unsigned int engine,
         LOG_FMD_CALL( FM_PCD_FrmReplicDeleteGroup, model->replicator_name[index] );
         err = FM_PCD_FrmReplicDeleteGroup( model->replicator_handle[index] );
         CHECK_ERR( FM_PCD_FrmReplicDeleteGroup, model->replicator_name[index] );
+        model->replicator_handle[index] = 0;
     }
 
     return 0;
diff --git a/source/libfmc.cpp b/source/libfmc.cpp
index 972f78f..6abe63d 100644
--- a/source/libfmc.cpp
+++ b/source/libfmc.cpp
@@ -287,8 +287,26 @@ int createDevices( fmc_model* pmodel )
     			}
     	    }
 
-    	    if (pmodel->port[index].env_id_handle)
-    			pmodel->port[index].env_id_handle = CreateDevice(pmodel->fman[current_engine].pcd_handle, pmodel->port[index].env_id_devId);
+            if (pmodel->port[index].vspParam.h_FmTxPort) {
+                fmPortParam.portType = pmodel->port[index].type == e_FM_PORT_TYPE_RX ?
+                    e_FM_PORT_TYPE_TX : e_FM_PORT_TYPE_TX_10G;
+                pmodel->port[index].vspParam.h_FmTxPort = FM_PORT_Open(&fmPortParam);
+                if (!pmodel->port[index].vspParam.h_FmTxPort)
+                    return 1;
+            }
+            if (pmodel->port[index].env_id_handle) {
+                for (j = 0; j < index; j++) {
+                    if (pmodel->port[j].env_id_handle &&
+                        pmodel->port[j].env_id_devId == pmodel->port[index].env_id_devId)
+                        break;
+                }
+                pmodel->port[index].env_id_handle = j < index ?
+                    pmodel->port[j].env_id_handle :
+                    CreateDevice(pmodel->fman[current_engine].pcd_handle,
+                                 pmodel->port[index].env_id_devId);
+                if (!pmodel->port[index].env_id_handle)
+                    return 1;
+            }
         	break;
 
         case FMCPortEnd:
@@ -359,74 +377,109 @@ void fmc_release( fmc_model* pmodel )
 
 			for (j = 0; j < pmodel->fman[index].reasm_count; j++)
 			{
-				if (pmodel->fman[index].reasm_handle[j])
-					ReleaseDevice(pmodel->fman[index].reasm_handle[j]);
+				if (pmodel->fman[index].reasm_handle[j]) {
+				    ReleaseDevice(pmodel->fman[index].reasm_handle[j]);
+				    pmodel->fman[index].reasm_handle[j] = 0;
+				}
 			}
 
 			for (j = 0; j < pmodel->fman[index].frag_count; j++)
 			{
-				if (pmodel->fman[index].frag_handle[j])
-					ReleaseDevice(pmodel->fman[index].frag_handle[j]);
+				if (pmodel->fman[index].frag_handle[j]) {
+				    ReleaseDevice(pmodel->fman[index].frag_handle[j]);
+				    pmodel->fman[index].frag_handle[j] = 0;
+				}
 			}
 
-			if (pmodel->fman[index].pcd_handle)
-				FM_PCD_Close(pmodel->fman[index].pcd_handle);
+			if (pmodel->fman[index].pcd_handle) {
+			    FM_PCD_Close(pmodel->fman[index].pcd_handle);
+			    pmodel->fman[index].pcd_handle = 0;
+			}
 
-			if (pmodel->fman[index].handle)
-				FM_Close(pmodel->fman[index].handle);
+			if (pmodel->fman[index].handle) {
+			    FM_Close(pmodel->fman[index].handle);
+			    pmodel->fman[index].handle = 0;
+			}
 			break;
 
 		case FMCEngineEnd:
+            current_engine = pmodel->apply_order[i].index;
 			break;
 
 		case FMCPortStart:
 			current_port = pmodel->apply_order[i].index;
 
-			if (pmodel->port[index].env_id_handle)
-				ReleaseDevice(pmodel->port[index].env_id_handle);
-
-			if (pmodel->port[index].handle)
-				FM_PORT_Close(pmodel->port[index].handle);
+            if (pmodel->port[index].env_id_handle) {
+                t_Handle env = pmodel->port[index].env_id_handle;
+                ReleaseDevice(env);
+                for (j = 0; j < pmodel->port_count; j++) {
+                    if (pmodel->port[j].env_id_handle == env)
+                        pmodel->port[j].env_id_handle = 0;
+                }
+            }
+            if (pmodel->port[index].vspParam.h_FmTxPort) {
+                FM_PORT_Close(pmodel->port[index].vspParam.h_FmTxPort);
+                pmodel->port[index].vspParam.h_FmTxPort = 0;
+            }
+
+			if (pmodel->port[index].handle) {
+			    FM_PORT_Close(pmodel->port[index].handle);
+			    pmodel->port[index].handle = 0;
+			}
 			break;
 
 		case FMCPortEnd:
 			break;
 
 		case FMCScheme:
-			if (pmodel->scheme_handle[index])
-				ReleaseDevice(pmodel->scheme_handle[index]);
+			if (pmodel->scheme_handle[index]) {
+			    ReleaseDevice(pmodel->scheme_handle[index]);
+			    pmodel->scheme_handle[index] = 0;
+			}
 			break;
 
 		case FMCCCNode:
-			if (pmodel->ccnode_handle[index])
-				ReleaseDevice(pmodel->ccnode_handle[index]);
+			if (pmodel->ccnode_handle[index]) {
+			    ReleaseDevice(pmodel->ccnode_handle[index]);
+			    pmodel->ccnode_handle[index] = 0;
+			}
 			break;
 
 		case FMCHTNode:
-			if (pmodel->htnode_handle[index])
-				ReleaseDevice(pmodel->htnode_handle[index]);
+			if (pmodel->htnode_handle[index]) {
+			    ReleaseDevice(pmodel->htnode_handle[index]);
+			    pmodel->htnode_handle[index] = 0;
+			}
 			break;
 
 #if (DPAA_VERSION >= 11)
 		case FMCReplicator:
-			if (pmodel->replicator_handle[index])
-				ReleaseDevice(pmodel->replicator_handle[index]);
+			if (pmodel->replicator_handle[index]) {
+			    ReleaseDevice(pmodel->replicator_handle[index]);
+			    pmodel->replicator_handle[index] = 0;
+			}
 			break;
 #endif /* (DPAA_VERSION >= 11) */
 
 		case FMCCCTree:
-			if (pmodel->port[index].cctree_handle)
-				ReleaseDevice(pmodel->port[index].cctree_handle);
+			if (pmodel->port[index].cctree_handle) {
+			    ReleaseDevice(pmodel->port[index].cctree_handle);
+			    pmodel->port[index].cctree_handle = 0;
+			}
 			break;
 
 		case FMCPolicer:
-			if (pmodel->policer_handle[index])
-				ReleaseDevice(pmodel->policer_handle[index]);
+			if (pmodel->policer_handle[index]) {
+			    ReleaseDevice(pmodel->policer_handle[index]);
+			    pmodel->policer_handle[index] = 0;
+			}
 			break;
 
 		case FMCManipulation:
-			if (pmodel->fman[current_engine].hdr_handle[index])
-				ReleaseDevice(pmodel->fman[current_engine].hdr_handle[index]);
+			if (pmodel->fman[current_engine].hdr_handle[index]) {
+			    ReleaseDevice(pmodel->fman[current_engine].hdr_handle[index]);
+			    pmodel->fman[current_engine].hdr_handle[index] = 0;
+			}
 			break;
 
 		default:
@@ -447,6 +500,10 @@ bool fmc_load( fmc_model* pmodel )
     }
 
     ifs.read( (char*)pmodel, sizeof( *pmodel ) );
+    if ( !ifs || pmodel->format_version != FMC_OUTPUT_FORMAT_VER ) {
+        fmc_log_write( LOG_ERR, "Invalid or incompatible saved FMC model" );
+        return false;
+    }
     ifs.close();
 
 	//Recreate old Linux device handles in the context of the current process:
