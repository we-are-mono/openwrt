From: Tomaz Zaman <tomaz@mono.si>
Date: Mon, 12 Jan 2026 12:00:00 +0100
Subject: [PATCH] fmlib: Add ASK timestamp and hash table extensions

Add FM_ReadTimeStamp() and FM_GetTimeStampIncrementPerUsec() functions
for timestamp reading from FMan engine.

Add hash table type enumeration and extended t_FmPcdHashTableParams
structure fields for external tables. Keep former reassembly fields
reserved for ABI compatibility.

Add FM_PCD_Get_Sch_handle() function for scheme handle access.

Add 'shared' field to t_FmPcdKgSchemeParams for shared scheme support.

These extensions are required for the CMM daemon and fast path offload
functionality on the Mono Gateway.

Retain the whole-tree replacement API as an unsupported operation in
Mono, returning before accessing handles or issuing an ioctl.

Reject unsupported hardware reassembly at the public API boundary,
before accessing object handles or sending ioctls.

Serialize the SDK direct-scheme flag into the public ioctl field; keep
the software-only shared flag out of the ioctl payload.

Copy the shared scheme tail through the enclosing SDK and ioctl objects
to make the intended span explicit to bounds checking.

Upstream-Status: Inappropriate [vendor-specific extension]
Signed-off-by: Tomaz Zaman <tomaz@mono.si>
---
diff --git a/include/fmd/Peripherals/fm_ext.h b/include/fmd/Peripherals/fm_ext.h
index 3607b86..e6db99a 100644
--- a/include/fmd/Peripherals/fm_ext.h
+++ b/include/fmd/Peripherals/fm_ext.h
@@ -581,6 +581,38 @@ t_Error FM_CtrlMonGetCounters(t_Handle h_Fm, uint8_t fmCtrlIndex, t_FmCtrlMon *p
 *//***************************************************************************/
 t_Error FM_ForceIntr (t_Handle h_Fm, e_FmExceptions exception);
 
+/**************************************************************************//*
+ @Function      FM_ReadTimeStamp
+
+ @Description   Reads the FMan engine's timestamp.
+
+ @Param[in]     h_Fm                A handle to an FM Module.
+
+ @Return        The indicated engine's timestamp on success; zero otherwise.
+
+ @Cautions      Allowed only following FM_Init().
+                This routine should NOT be called from guest-partition
+                (i.e. guestId != NCSW_MASTER_ID)
+*//***************************************************************************/
+uint32_t FM_ReadTimeStamp(t_Handle h_Fm);
+
+/**************************************************************************//*
+ @Function      FM_GetTimeStampIncrementPerUsec
+
+ @Description   Provides the value of the FMan engine's timestamp increment
+                per microsecond.
+
+ @Param[in]     h_Fm                A handle to an FM Module.
+
+ @Return        The value the timestamp is incremented with each microsecond
+                on success; zero otherwise.
+
+ @Cautions      Allowed only following FM_Init().
+                This routine should NOT be called from guest-partition
+                (i.e. guestId != NCSW_MASTER_ID)
+*//***************************************************************************/
+uint32_t FM_GetTimeStampIncrementPerUsec(t_Handle h_Fm);
+
 /** @} */ /* end of lnx_usr_FM_runtime_control_grp group */
 /** @} */ /* end of lnx_usr_FM_lib_grp group */
 /** @} */ /* end of lnx_usr_FM_grp group */
diff --git a/include/fmd/Peripherals/fm_pcd_ext.h b/include/fmd/Peripherals/fm_pcd_ext.h
index 7f32680..ea02334 100644
--- a/include/fmd/Peripherals/fm_pcd_ext.h
+++ b/include/fmd/Peripherals/fm_pcd_ext.h
@@ -1254,7 +1254,7 @@ typedef enum e_FmPcdStatsType {
 *//***************************************************************************/
 typedef enum e_FmPcdManipType {
     e_FM_PCD_MANIP_HDR = 0,             /**< Header manipulation */
-    e_FM_PCD_MANIP_REASSEM,             /**< Reassembly */
+    e_FM_PCD_MANIP_REASSEM,            /**< Reserved in Mono; hardware reassembly is unsupported. */
     e_FM_PCD_MANIP_FRAG,                /**< Fragmentation */
     e_FM_PCD_MANIP_SPECIAL_OFFLOAD      /**< Special Offloading */
 } e_FmPcdManipType;
@@ -1717,6 +1717,7 @@ typedef struct t_FmPcdKgSchemeParams {
         uint8_t                         relativeSchemeId;       /**< if modify=FALSE:Partition relative scheme id */
         t_Handle                        h_Scheme;               /**< if modify=TRUE: a handle of the existing scheme */
     } id;
+    bool                                shared;           	/**< This scheme is  shared */
     bool                                alwaysDirect;           /**< This scheme is reached only directly, i.e. no need
                                                                      for match vector; KeyGen will ignore it when matching */
     struct {                                                    /**< HL Relevant only if alwaysDirect = FALSE */
@@ -1974,6 +1975,27 @@ typedef struct t_FmPcdCcNodeParams {
 /**************************************************************************//**
  @Description   Parameters for defining a hash table
 *//***************************************************************************/
+//sync with fm_eh_types.h
+enum {
+        IPV4_UDP_TABLE,
+        IPV4_TCP_TABLE,
+        IPV6_UDP_TABLE,
+        IPV6_TCP_TABLE,
+        ESP_IPV4_TABLE,
+        ESP_IPV6_TABLE,
+        IPV4_MULTICAST_TABLE,
+        IPV6_MULTICAST_TABLE,
+        PPPOE_RELAY_TABLE,
+        ETHERNET_TABLE,
+        IPV4_3TUPLE_UDP_TABLE,
+        IPV4_3TUPLE_TCP_TABLE,
+        IPV6_3TUPLE_UDP_TABLE,
+        IPV6_3TUPLE_TCP_TABLE,
+        IPV4_REASSM_TABLE,
+        IPV6_REASSM_TABLE,
+        MAX_MATCH_TABLES
+};
+
 typedef struct t_FmPcdHashTableParams {
     uint16_t                    maxNumOfKeys;               /**< Maximum Number Of Keys that will (ever) be used in this Hash-table */
     e_FmPcdCcStatsMode          statisticsMode;             /**< If not e_FM_PCD_CC_STATS_MODE_NONE, the required structures for the
@@ -1990,6 +2012,29 @@ typedef struct t_FmPcdHashTableParams {
 
     t_FmPcdCcNextEngineParams   ccNextEngineParamsForMiss;  /**< Parameters for defining the next engine when a key is not matched */
 
+    bool                        agingSupport;               /**< Reserved; hash-key aging is not implemented. Kept so this struct
+                                                                 stays layout-identical to the driver's t_FmPcdHashTableParams and
+                                                                 to ioc_fm_pcd_hash_table_params_t, which FM_PCD_HashTableSet()
+                                                                 memcpy()s this struct into. */
+
+#if (DPAA_VERSION >= 11)
+    bool                        externalHash;               /**< TRUE to use external hash table */
+
+    uint32_t                    table_type;                 /**< ip reassembly table type */
+    struct {
+        uint32_t                timeout_val;                /**< reassembly timeout */
+        uint32_t                timeout_fqid;               /**< fqid for reassembly failures */
+        uint32_t                max_frags;                  /**< max allowed fragments */
+        uint32_t                min_frag_size;              /**< min allowed frag size except last frag */
+        uint32_t                max_sessions;               /**< max conn reassembly sessions */
+    };
+
+    struct {
+        uint8_t                 dataMemId;                  /**< Memory partition ID for external hash table */
+        uint16_t                dataLiodnOffs;              /**< LIODN offset for external hash access */
+        uintptr_t               missMonitorAddr;            /**< User-allocated miss monitor address */
+    } externalHashParams;
+#endif /* (DPAA_VERSION >= 11) */
 } t_FmPcdHashTableParams;
 
 /**************************************************************************//**
diff --git a/include/fmd/Peripherals/fm_port_ext.h b/include/fmd/Peripherals/fm_port_ext.h
index df72794..e40af4a 100644
--- a/include/fmd/Peripherals/fm_port_ext.h
+++ b/include/fmd/Peripherals/fm_port_ext.h
@@ -1732,6 +1732,9 @@ t_Error FM_PORT_Disable(t_Handle h_FmPort);
 *//***************************************************************************/
 t_Error FM_PORT_Enable(t_Handle h_FmPort);
 
+/* Query the initialized port's current enable state without changing it. */
+t_Error FM_PORT_GetEnabled(t_Handle h_FmPort, bool *p_Enabled);
+
 /**************************************************************************//**
  @Function      FM_PORT_SetRateLimit
 
@@ -2270,9 +2273,9 @@ typedef struct t_FmPortPcdParams {
                                                      or if any flow uses a KG scheme were policer
                                                      profile is not generated
                                                      ('bypassPlcrProfileGeneration selected'). */
-    t_Handle                h_IpReassemblyManip;    /**< IP Reassembly manipulation */
+    t_Handle                h_IpReassemblyManip; /**< Reserved in Mono; must be NULL. */
 #if (DPAA_VERSION >= 11)
-    t_Handle                h_CapwapReassemblyManip;/**< CAPWAP Reassembly manipulation */
+    t_Handle                h_CapwapReassemblyManip; /**< Reserved in Mono; must be NULL. */
 #endif /* (DPAA_VERSION >= 11) */
 } t_FmPortPcdParams;
 
@@ -2462,18 +2465,13 @@ t_Error FM_PORT_PcdPlcrModifyInitialProfile (t_Handle h_FmPort, t_Handle h_Profi
 /**************************************************************************//**
  @Function      FM_PORT_PcdCcModifyTree
 
- @Description   This routine may be called for ports that use coarse classification tree
-                if the user wishes to replace the tree. The routine may not be called while port
-                receives packets using the PCD functionalities, therefor port must be first detached
-                from the PCD, only than the routine may be called, and than port be attached to PCD again.
-
- @Param[in]     h_FmPort            A handle to a FM Port module.
- @Param[in]     h_CcTree            A CC tree that was already built. The tree id as returned from
-                                    the BuildTree routine.
+ @Description   Whole-tree replacement is unsupported in Mono. This entry point
+                is retained for API compatibility and does not access either handle.
 
- @Return        E_OK on success; Error code otherwise.
+ @Param[in]     h_FmPort            Unused port handle.
+ @Param[in]     h_CcTree            Unused tree handle.
 
- @Cautions      Allowed only following FM_PORT_Init(), FM_PORT_SetPCD() and FM_PORT_DetachPCD()
+ @Return        E_NOT_SUPPORTED.
 *//***************************************************************************/
 t_Error FM_PORT_PcdCcModifyTree (t_Handle h_FmPort, t_Handle h_CcTree);
 
diff --git a/src/fm_lib.c b/src/fm_lib.c
index 4de5168..50fdbf5 100644
--- a/src/fm_lib.c
+++ b/src/fm_lib.c
@@ -317,6 +317,44 @@ t_Error  FM_GetApiVersion(t_Handle h_Fm, ioc_fm_api_version_t *p_version)
     return E_OK;
 }
 
+uint32_t FM_ReadTimeStamp(t_Handle h_Fm)
+{
+    t_Device    *p_Dev = (t_Device*) h_Fm;
+    uint32_t    ts = 0;
+
+    SANITY_CHECK_RETURN_ERROR(p_Dev, E_INVALID_HANDLE);
+
+    _fml_dbg("Calling...\n");
+
+    if (ioctl(p_Dev->fd, FM_IOC_READ_TIMESTAMP, &ts)) {
+        REPORT_ERROR(MAJOR, E_INVALID_OPERATION, NO_MSG);
+        return 0;
+    }
+
+    _fml_dbg("Called.\n");
+
+    return ts;
+}
+
+uint32_t FM_GetTimeStampIncrementPerUsec(t_Handle h_Fm)
+{
+    t_Device    *p_Dev = (t_Device*) h_Fm;
+    uint32_t    ts_inc = 0;
+
+    SANITY_CHECK_RETURN_ERROR(p_Dev, E_INVALID_HANDLE);
+
+    _fml_dbg("Calling...\n");
+
+    if (ioctl(p_Dev->fd, FM_IOC_GET_TIMESTAMP_INCREMENT, &ts_inc)) {
+        REPORT_ERROR(MAJOR, E_INVALID_OPERATION, NO_MSG);
+        return 0;
+    }
+
+    _fml_dbg("Called.\n");
+
+    return ts_inc;
+}
+
 /********************************************************************************************/
 /*  FM_PCD FUNCTIONS                                                                        */
 /********************************************************************************************/
@@ -556,18 +594,20 @@ t_Handle FM_PCD_NetEnvCharacteristicsSet(t_Handle h_FmPcd, t_FmPcdNetEnvParams *
     memcpy(&params, p_NetEnvParams, sizeof(t_FmPcdNetEnvParams));
     params.id = NULL;
 
-    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_NET_ENV_CHARACTERISTICS_SET, &params))
+    p_Dev = (t_Device *)malloc(sizeof(t_Device));
+    if (!p_Dev)
     {
-        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
+        REPORT_ERROR(MAJOR, E_NO_MEMORY, ("FM PCD NetEnv Chrs device!!"));
         return NULL;
     }
 
-    p_Dev = (t_Device *)malloc(sizeof(t_Device));
-    if (!p_Dev)
+    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_NET_ENV_CHARACTERISTICS_SET, &params))
     {
-        REPORT_ERROR(MAJOR, E_NO_MEMORY, ("FM PCD NetEnv Chrs device!!"));
+        free(p_Dev);
+        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
         return NULL;
     }
+
     memset(p_Dev, 0, sizeof(t_Device));
     p_Dev->h_UserPriv = (t_Handle)p_PcdDev;
     p_PcdDev->owners++;
@@ -624,6 +664,12 @@ t_Error FM_PCD_AllowHcUsage(t_Handle h_FmPcd, bool allow)
     return E_OK;
 }
 
+void *FM_PCD_Get_Sch_handle(t_Handle h_Scheme)
+{
+    t_Device *p_Dev = (t_Device*) h_Scheme;
+    return(UINT_TO_PTR(p_Dev->id));
+}
+
 t_Handle FM_PCD_KgSchemeSet (t_Handle h_FmPcd, t_FmPcdKgSchemeParams *p_Scheme)
 {
     t_Device *p_PcdDev = (t_Device*) h_FmPcd;
@@ -634,8 +680,18 @@ t_Handle FM_PCD_KgSchemeSet (t_Handle h_FmPcd, t_FmPcdKgSchemeParams *p_Scheme)
 
     _fml_dbg("Calling...\n");
 
-    memcpy(&params, p_Scheme, sizeof(t_FmPcdKgSchemeParams));
-    params.id = NULL;
+    /* shared belongs to the SDK/FMC model, not the public ioctl. */
+    memset(&params, 0, sizeof(params));
+    _Static_assert(offsetof(ioc_fm_pcd_kg_scheme_params_t, id) -
+                   offsetof(ioc_fm_pcd_kg_scheme_params_t, net_env_params) ==
+                   sizeof(*p_Scheme) - offsetof(t_FmPcdKgSchemeParams, netEnvParams),
+                   "SDK and ioctl scheme tails must have identical layouts");
+    memcpy(&params, p_Scheme, offsetof(t_FmPcdKgSchemeParams, shared));
+    params.always_direct = p_Scheme->alwaysDirect;
+    /* Use the containing objects for the shared tail's bounds. */
+    memcpy((char *)&params + offsetof(ioc_fm_pcd_kg_scheme_params_t, net_env_params),
+           (char *)p_Scheme + offsetof(t_FmPcdKgSchemeParams, netEnvParams),
+           sizeof(*p_Scheme) - offsetof(t_FmPcdKgSchemeParams, netEnvParams));
 
     if (params.modify)
     {
@@ -657,19 +713,25 @@ t_Handle FM_PCD_KgSchemeSet (t_Handle h_FmPcd, t_FmPcdKgSchemeParams *p_Scheme)
         params.kg_next_engine_params.cc.tree_id)
             DEV_TO_ID(params.kg_next_engine_params.cc.tree_id);
 
-    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_KG_SCHEME_SET, &params))
+    if (params.modify)
+        p_Dev = (t_Device *)p_Scheme->id.h_Scheme;
+    else
+        p_Dev = (t_Device *)malloc(sizeof(t_Device));
+    if (!p_Dev)
     {
-        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
+        REPORT_ERROR(MAJOR, E_NO_MEMORY, ("FM PCD KG Scheme device!!"));
         return NULL;
     }
 
-    p_Dev = (t_Device *)malloc(sizeof(t_Device));
-    if (!p_Dev)
+    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_KG_SCHEME_SET, &params))
     {
-        REPORT_ERROR(MAJOR, E_NO_MEMORY, ("FM PCD KG Scheme device!!"));
+        if (!params.modify)
+            free(p_Dev);
+        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
         return NULL;
     }
 
+
     memset(p_Dev, 0, sizeof(t_Device));
     p_Dev->h_UserPriv = (t_Handle)p_PcdDev;
     /* increase owners only if a new scheme is created */
@@ -765,19 +827,21 @@ t_Handle FM_PCD_CcRootBuild(t_Handle h_FmPcd, t_FmPcdCcTreeParams *p_PcdTreePara
             }
         }
 
-    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_CC_ROOT_BUILD, &params))
+    p_Dev = (t_Device *)malloc(sizeof(t_Device));
+    if (!p_Dev)
     {
-        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
+        REPORT_ERROR(MAJOR, E_NO_MEMORY, ("FM PCD CC Root device!!"));
         return NULL;
     }
 
-    p_Dev = (t_Device *)malloc(sizeof(t_Device));
-    if (!p_Dev)
+    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_CC_ROOT_BUILD, &params))
     {
-        REPORT_ERROR(MAJOR, E_NO_MEMORY, ("FM PCD CC Root device!!"));
+        free(p_Dev);
+        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
         return NULL;
     }
 
+
     memset(p_Dev, 0, sizeof(t_Device));
     p_Dev->h_UserPriv = (t_Handle)p_PcdDev;
     p_PcdDev->owners++;
@@ -865,18 +929,20 @@ t_Handle FM_PCD_MatchTableSet(t_Handle h_FmPcd, t_FmPcdCcNodeParams *p_CcNodePar
 
 #endif /* DPAA_VERSION >= 11 */
 
-    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_MATCH_TABLE_SET, &params))
+    p_Dev = (t_Device *)malloc(sizeof(t_Device));
+    if (!p_Dev)
     {
-        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
+        REPORT_ERROR(MAJOR, E_NO_MEMORY, ("FM PCD Match Table device!!"));
         return NULL;
     }
 
-    p_Dev = (t_Device *)malloc(sizeof(t_Device));
-    if (!p_Dev)
+    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_MATCH_TABLE_SET, &params))
     {
-        REPORT_ERROR(MAJOR, E_NO_MEMORY, ("FM PCD Match Table device!!"));
+        free(p_Dev);
+        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
         return NULL;
     }
+
     memset(p_Dev, 0, sizeof(t_Device));
     p_Dev->h_UserPriv = (t_Handle)p_PcdDev;
     p_PcdDev->owners++;
@@ -969,48 +1035,12 @@ t_Error FM_PCD_CcRootModifyNextEngine(t_Handle                  h_CcTree,
                                       uint8_t                   index,
                                       t_FmPcdCcNextEngineParams *p_FmPcdCcNextEngineParams)
 {
-    t_Device *p_Dev = (t_Device*) h_CcTree;
-    t_Device *p_PcdDev = NULL;
-    ioc_fm_pcd_cc_tree_modify_next_engine_params_t  params;
-
-    ASSERT_COND(sizeof(t_FmPcdCcNextEngineParams) == sizeof(ioc_fm_pcd_cc_next_engine_params_t));
-    SANITY_CHECK_RETURN_ERROR(p_Dev, E_INVALID_HANDLE);
-
-    _fml_dbg("Calling...\n");
-
-    p_PcdDev = (t_Device *)p_Dev->h_UserPriv;
-
-    params.id = UINT_TO_PTR(p_Dev->id);
-    params.grp_indx = grpId;
-    params.indx = index;
-    memcpy(&params.cc_next_engine_params, p_FmPcdCcNextEngineParams, sizeof(t_FmPcdCcNextEngineParams));
-    if (p_FmPcdCcNextEngineParams->nextEngine == e_FM_PCD_CC) {
-        t_Device *p_NextDev = (t_Device*) p_FmPcdCcNextEngineParams->params.ccParams.h_CcNode;
-        params.cc_next_engine_params.params.cc_params.cc_node_id = UINT_TO_PTR(p_NextDev->id);
-    }
-	if (p_FmPcdCcNextEngineParams->nextEngine == e_FM_PCD_KG) {
-        t_Device *p_NextDev = (t_Device*) p_FmPcdCcNextEngineParams->params.kgParams.h_DirectScheme;
-        params.cc_next_engine_params.params.kg_params.p_direct_scheme = UINT_TO_PTR(p_NextDev->id);
-    }
-
-#if (DPAA_VERSION >= 11)
-	if (p_FmPcdCcNextEngineParams->nextEngine == e_FM_PCD_FR) {
-        t_Device *p_NextDev = (t_Device*) p_FmPcdCcNextEngineParams->params.frParams.h_FrmReplic;
-        params.cc_next_engine_params.params.fr_params.frm_replic_id = UINT_TO_PTR(p_NextDev->id);
-    }
-#endif /* DPAA_VERSION >= 11 */
-
-	if (p_FmPcdCcNextEngineParams->h_Manip) {
-		t_Device *p_NextDev = (t_Device*) p_FmPcdCcNextEngineParams->h_Manip;
-		params.cc_next_engine_params.manip_id = UINT_TO_PTR(p_NextDev->id);
-	}
-
-	if (ioctl(p_PcdDev->fd, FM_PCD_IOC_CC_ROOT_MODIFY_NEXT_ENGINE, &params))
-        RETURN_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
-
-    _fml_dbg("Called.\n");
-
-    return E_OK;
+    /* External-hash roots do not support the legacy CC-node retargeting path. */
+    (void)h_CcTree;
+    (void)grpId;
+    (void)index;
+    (void)p_FmPcdCcNextEngineParams;
+    RETURN_ERROR(MINOR, E_NOT_SUPPORTED, NO_MSG);
 }
 
 t_Error FM_PCD_MatchTableModifyNextEngine(t_Handle                  h_CcNode,
@@ -1191,6 +1221,15 @@ t_Error FM_PCD_MatchTableModifyKeyAndNextEngine(t_Handle            h_CcNode,
     return E_OK;
 }
 
+/* The SDK masks external-hash types to the low four bits. */
+#define FM_PCD_HASH_TABLE_TYPE_MASK 0xf
+
+/* The ioctl struct is this struct plus a trailing 'void *id'; the memcpy below
+ * relies on the two being field-for-field identical up to that point. */
+_Static_assert(sizeof(ioc_fm_pcd_hash_table_params_t) ==
+               sizeof(t_FmPcdHashTableParams) + sizeof(void *),
+               "t_FmPcdHashTableParams and ioc_fm_pcd_hash_table_params_t layouts diverged");
+
 t_Handle FM_PCD_HashTableSet(t_Handle h_FmPcd, t_FmPcdHashTableParams *p_Param)
 {
     t_Device *p_PcdDev = (t_Device*) h_FmPcd;
@@ -1199,8 +1238,17 @@ t_Handle FM_PCD_HashTableSet(t_Handle h_FmPcd, t_FmPcdHashTableParams *p_Param)
 
     SANITY_CHECK_RETURN_VALUE(p_PcdDev, E_INVALID_HANDLE, NULL);
 
+    SANITY_CHECK_RETURN_VALUE(p_Param, E_NULL_POINTER, NULL);
+    if ((p_Param->table_type & FM_PCD_HASH_TABLE_TYPE_MASK) == IPV4_REASSM_TABLE ||
+        (p_Param->table_type & FM_PCD_HASH_TABLE_TYPE_MASK) == IPV6_REASSM_TABLE)
+    {
+        REPORT_ERROR(MAJOR, E_NOT_SUPPORTED, ("Hardware reassembly is unsupported in Mono"));
+        return NULL;
+    }
+
     _fml_dbg("Calling...\n");
 
+    memset(&params, 0, sizeof(params));
     memcpy(&params, p_Param, sizeof(t_FmPcdHashTableParams));
     params.id = NULL;
 
@@ -1222,18 +1270,20 @@ t_Handle FM_PCD_HashTableSet(t_Handle h_FmPcd, t_FmPcdHashTableParams *p_Param)
 
 #endif /* DPAA_VERSION >= 11 */
 
-    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_HASH_TABLE_SET, &params))
+    p_Dev = (t_Device *)malloc(sizeof(t_Device));
+    if (!p_Dev)
     {
-        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
+        REPORT_ERROR(MAJOR, E_NO_MEMORY, ("FM PCD Hash Table device!!"));
         return NULL;
     }
 
-    p_Dev = (t_Device *)malloc(sizeof(t_Device));
-    if (!p_Dev)
+    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_HASH_TABLE_SET, &params))
     {
-        REPORT_ERROR(MAJOR, E_NO_MEMORY, ("FM PCD Hash Table device!!"));
+        free(p_Dev);
+        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
         return NULL;
     }
+
     memset(p_Dev, 0, sizeof(t_Device));
     p_Dev->h_UserPriv = (t_Handle)p_PcdDev;
     p_PcdDev->owners++;
@@ -1423,19 +1473,21 @@ t_Handle FM_PCD_PlcrProfileSet(t_Handle h_FmPcd, t_FmPcdPlcrProfileParams *p_Pro
     if (params.next_engine_on_red == e_IOC_FM_PCD_KG && params.params_on_red.p_direct_scheme)
         DEV_TO_ID(params.params_on_red.p_direct_scheme);
 
-    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_PLCR_PROFILE_SET, &params))
+    p_Dev = (t_Device *)malloc(sizeof(t_Device));
+    if (!p_Dev)
     {
-        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
+        REPORT_ERROR(MAJOR, E_NO_MEMORY, ("FM PCD Policer Profile device!!"));
         return NULL;
     }
 
-    p_Dev = (t_Device *)malloc(sizeof(t_Device));
-    if (!p_Dev)
+    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_PLCR_PROFILE_SET, &params))
     {
-        REPORT_ERROR(MAJOR, E_NO_MEMORY, ("FM PCD Policer Profile device!!"));
+        free(p_Dev);
+        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
         return NULL;
     }
 
+
     memset(p_Dev, 0, sizeof(t_Device));
     p_Dev->h_UserPriv = (t_Handle)p_PcdDev;
     p_PcdDev->owners++;
@@ -1479,6 +1531,13 @@ t_Handle FM_PCD_ManipNodeSet(t_Handle h_FmPcd, t_FmPcdManipParams *p_FmPcdManipP
 
     SANITY_CHECK_RETURN_VALUE(p_PcdDev, E_INVALID_HANDLE, NULL);
 
+    SANITY_CHECK_RETURN_VALUE(p_FmPcdManipParams, E_NULL_POINTER, NULL);
+    if (p_FmPcdManipParams->type == e_FM_PCD_MANIP_REASSEM)
+    {
+        REPORT_ERROR(MAJOR, E_NOT_SUPPORTED, ("Hardware reassembly is unsupported in Mono"));
+        return NULL;
+    }
+
     _fml_dbg("Calling...\n");
 
     memset(&params, 0, sizeof(ioc_fm_pcd_manip_params_t));
@@ -1489,11 +1548,6 @@ t_Handle FM_PCD_ManipNodeSet(t_Handle h_FmPcd, t_FmPcdManipParams *p_FmPcdManipP
         params.p_next_manip = UINT_TO_PTR(p_Dev->id);
     }
 
-    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_MANIP_NODE_SET, &params)){
-        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
-        return NULL;
-    }
-
     p_Dev = (t_Device *)malloc(sizeof(t_Device));
     if (!p_Dev)
     {
@@ -1501,6 +1555,13 @@ t_Handle FM_PCD_ManipNodeSet(t_Handle h_FmPcd, t_FmPcdManipParams *p_FmPcdManipP
         return NULL;
     }
 
+    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_MANIP_NODE_SET, &params)){
+        free(p_Dev);
+        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
+        return NULL;
+    }
+
+
     memset(p_Dev, 0, sizeof(t_Device));
     p_Dev->h_UserPriv = (t_Handle)p_PcdDev;
     p_PcdDev->owners++;
@@ -1716,6 +1777,19 @@ t_Error FM_PORT_Disable(t_Handle h_FmPort)
     return E_OK;
 }
 
+t_Error FM_PORT_GetEnabled(t_Handle h_FmPort, bool *p_Enabled)
+{
+    t_Device *p_Dev = (t_Device *)h_FmPort;
+    uint8_t enabled;
+
+    SANITY_CHECK_RETURN_ERROR(p_Dev, E_INVALID_HANDLE);
+    SANITY_CHECK_RETURN_ERROR(p_Enabled, E_NULL_POINTER);
+    if (ioctl(p_Dev->fd, FM_PORT_IOC_GET_ENABLED, &enabled))
+        RETURN_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
+    *p_Enabled = !!enabled;
+    return E_OK;
+}
+
 t_Error FM_PORT_Enable(t_Handle h_FmPort)
 {
     t_Device    *p_Dev = (t_Device *)h_FmPort;
@@ -1830,6 +1904,12 @@ t_Error FM_PORT_SetPCD(t_Handle h_FmPort, t_FmPortPcdParams *p_FmPortPcd)
     ASSERT_COND(sizeof(t_FmPortPcdParams) == sizeof(ioc_fm_port_pcd_params_t));
     SANITY_CHECK_RETURN_ERROR(p_Dev, E_INVALID_HANDLE);
 
+    SANITY_CHECK_RETURN_ERROR(p_FmPortPcd, E_NULL_POINTER);
+    if (p_FmPortPcd->h_IpReassemblyManip || p_FmPortPcd->h_CapwapReassemblyManip)
+    {
+        return ERROR_CODE(E_NOT_SUPPORTED);
+    }
+
     _fml_dbg("Calling...\n");
 
     memcpy(&params, p_FmPortPcd, sizeof(t_FmPortPcdParams));
@@ -1881,14 +1961,6 @@ t_Error FM_PORT_SetPCD(t_Handle h_FmPort, t_FmPortPcdParams *p_FmPortPcd)
         }
     }
 
-    if (params.p_ip_reassembly_manip)
-        DEV_TO_ID(params.p_ip_reassembly_manip);
-
-#if (DPAA_VERSION >= 11)
-    if (params.p_capwap_reassembly_manip)
-	DEV_TO_ID(params.p_capwap_reassembly_manip);
-#endif
-
     if (ioctl(p_Dev->fd, FM_PORT_IOC_SET_PCD, &params))
         RETURN_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
 
@@ -2013,23 +2085,12 @@ t_Error FM_PORT_PcdPlcrModifyInitialProfile (t_Handle h_FmPort, t_Handle h_Profi
     return E_OK;
 }
 
-t_Error FM_PORT_PcdCcModifyTree (t_Handle h_FmPort, t_Handle h_CcTree)
+t_Error FM_PORT_PcdCcModifyTree(t_Handle h_FmPort, t_Handle h_CcTree)
 {
-    t_Device *p_Dev = (t_Device*) h_FmPort;
-    ioc_fm_obj_t id;
-
-    SANITY_CHECK_RETURN_ERROR(p_Dev, E_INVALID_HANDLE);
-
-    _fml_dbg("Calling...\n");
-
-    id.obj = h_CcTree;
-
-    if (ioctl(p_Dev->fd, FM_PORT_IOC_PCD_CC_MODIFY_TREE, &id))
-        RETURN_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
-
-    _fml_dbg("Called.\n");
-
-    return E_OK;
+    /* Keep the API entry point, but never mutate a tree or port. */
+    (void)h_FmPort;
+    (void)h_CcTree;
+    return ERROR_CODE(E_NOT_SUPPORTED);
 }
 
 t_Error FM_PORT_PcdKgBindSchemes (t_Handle h_FmPort, t_FmPcdPortSchemesParams *p_PortScheme)
@@ -2127,18 +2188,20 @@ t_Handle FM_PCD_FrmReplicSetGroup(t_Handle h_FmPcd, t_FmPcdFrmReplicGroupParams
         }
     }
 
-    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_FRM_REPLIC_GROUP_SET, &params))
+    p_Dev = (t_Device *)malloc(sizeof(t_Device));
+    if (!p_Dev)
     {
-        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
+        REPORT_ERROR(MAJOR, E_NO_MEMORY, ("FM PCD NetEnv Chrs device!!"));
         return NULL;
     }
 
-    p_Dev = (t_Device *)malloc(sizeof(t_Device));
-    if (!p_Dev)
+    if (ioctl(p_PcdDev->fd, FM_PCD_IOC_FRM_REPLIC_GROUP_SET, &params))
     {
-        REPORT_ERROR(MAJOR, E_NO_MEMORY, ("FM PCD NetEnv Chrs device!!"));
+        free(p_Dev);
+        REPORT_ERROR(MINOR, E_INVALID_OPERATION, NO_MSG);
         return NULL;
     }
+
     memset(p_Dev, 0, sizeof(t_Device));
     p_Dev->h_UserPriv = (t_Handle)p_PcdDev;
     p_PcdDev->owners++;
